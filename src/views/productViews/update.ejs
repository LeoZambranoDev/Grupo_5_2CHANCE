<!DOCTYPE html>
<html lang="es">

<head>

  <!-- metas and lynks -->
  <%- include('../partials/metasBasics') %>
    <!-- metas and lynks -->

    <link rel="stylesheet" href="/css/product/update.css" />
    <title>Inicio</title>
</head>

<body>

  <!-- header -->
  <%- include('../partials/header') %>
    <!-- header -->

    <main>
      <div class="container-product-update">
        <div class="title-update">
          EDITAR PRODUCTO: <%= product.name %>
        </div>
        <form class="form-product-update" action="/product/<%= product.id %>?_method=PUT" method="POST"
          enctype="multipart/form-data">
          <input type="text" name="name" placeholder="Nombre del producto" value="<%= product.name %>">
          
          <select class="select" name="category" id="">
            <option value="">Categoría:</option>
            <% if (locals.categories) { %>

              <% categories.forEach(element=> { %>
                <option <% if (product.category_id == element.id ) { %>selected<% } %> value="<%= element.id %> "><%= element.name %> </option>
              <% }) %>

            <% } %>
          </select>

          <select class="select" name="color" id="">
            <option value="">Color:</option>

            <% if (locals.colors) { %>

              <% colors.forEach(element=> { %>
                <option <% if (product.color_id == element.id ) { %>selected<% } %> value="<%= element.id %> "><%= element.name %> </option>
              <% }) %>

            <% } %>
            
          </select>
          <!-- <input type="text" placeholder="Marca"> -->
          <select class="select" name="mark" id="">
            <option value="">Marca:</option>
            <% if (locals.brands) { %>

              <% brands.forEach(element=> { %>
                <option <% if (product.brand_id == element.id ) { %>selected<% } %> value="<%= element.id %> "><%= element.name %> </option>
              <% }) %>

            <% } %>
          </select>
          <!-- <input type="text" placeholder="Memoria"> -->
          <select class="select" name="memory" id="">
            <option value="">Memoria:</option>
            <% if (locals.memories) { %>

              <% memories.forEach(element=> { %>
                <option <% if (product.memory_id == element.id ) { %>selected<% } %> value="<%= element.id %> "><%= element.name %> </option>
              <% }) %>

            <% } %>
          </select>

          <select class="select" name="ram" id="">
            <option value="">Ram:</option>
            <% if (locals.rams) { %>

              <% rams.forEach(element=> { %>
                <option <% if (product.ram_id == element.id ) { %>selected<% } %> value="<%= element.id %> "><%= element.name %> </option>
              <% }) %>

            <% } %>
          </select>
          <input type="text" name="price" placeholder="Precio" value="<%= product.price %>">
          <snap class="file">Imagen del dispositivo:<input type="file" class="input-file" name="imgCel" id="imgsCel"
              accept="image/jpg, image/jpeg, image/png"></snap>
          <img src="/img/products/<%= product.product_img %> " alt="">
          <div class="btn-content"><button type="submit">EDITAR</button></div>
          <input class="oculto" type="text" value="<%= product.product_img %>" name="imgCel_old">
        </form>
      </div>
    </main>

    <!-- footer -->
    <%- include('../partials/footer') %>
      <!-- footer -->

</body>

</html>