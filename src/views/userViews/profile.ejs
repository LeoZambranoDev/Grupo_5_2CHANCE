<!DOCTYPE html>
<html lang="es">

<head>
    
    <!-- metas and lynks -->
    <%- include('../partials/metasBasics') %>
    <link rel="stylesheet" href="/css/user/styleRegister.css">
    <title>Mi perfil</title>
</head>

<body>
	<!-- metas and lynks -->
    <%- include('../partials/header') %>
	<!--perfil y logout -->


	<!-- Container -->
	<main>

		<div class="contentUser">
            <form class="formUpdate" action="/user/register" method="POST" enctype="multipart/form-data">
                <div class="title">INFORMACIÓN PERSONAL</div>

                <div class="errorsList">
					<% if (typeof errorsList !='undefined') { %>
                    <% errorsList.forEach(element => { %>
                        <div class="error">- <%= element.msg %></div>
                    <% }) %> 
                    <% } %> 
                </div>
				<label for="nick">Nick</label>
                <input type="text" name="nick" id="nick" placeholder="Ingresa tu usuario" disabled value="<%= locals.userLogged.nick %>"> 
                <label for="email">Email</label>
				<input type="email" name="email" id="email" placeholder="Ingresa tu correo" disabled value="<%= locals.userLogged.email %>" >
                <!-- <input type="password" name="pass" placeholder="Ingresa tu contraseña">
                <input type="password" name="passConfirm" placeholder="Confirma tu contraseña"> -->
                <label for="firstName">Nombres</label>
				<input type="text" name="firstName" id="firstName" placeholder=""    value="<%= (locals.userLogged.firstName)?locals.userLogged.firstName:'' %>" > 

				<label for="lastName">Apellidos</label>
				<input type="text" name="lastName" id="lastName" placeholder=""    value="<%= (locals.userLogged.lastName)?locals.userLogged.lastName:'' %>" > 

				<label for="address">Dirección</label>
				<input type="text" name="address" id="address" placeholder=""    value="<%= (locals.userLogged.address)?locals.userLogged.address:'' %>" > 
                <label for="type">Tipo de usuario</label>
				<input type="text" name="" id="type" placeholder="" disabled value="<%= (locals.userLogged.type)?locals.userLogged.type:'' %>" > 

                <snap class="file">Cambiar imagen de perfil:<input class="input-file" name="img-perfil" type="file"></snap>
                <button type="submit">Actualizar</button>
            </form>
        </div>

	</main>
	<!-- //Container -->
	
    <%- include('../partials/footer') %>
</body>
</html>